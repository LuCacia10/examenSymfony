{% extends 'base.html.twig' %}

{% block title %}Cotisation{% endblock %}

{% block body %}
    <h1>Cotisation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cotisation.id }}</td>
            </tr>
            <tr>
                <th>Amount</th>
                <td>{{ cotisation.amount }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ cotisation.date ? cotisation.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>IsPaid</th>
                <td>{{ cotisation.isPaid ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_cotisation_index') }}">back to list</a>

    <a href="{{ path('app_cotisation_edit', {'id': cotisation.id}) }}">edit</a>

    {{ include('cotisation/_delete_form.html.twig') }}
{% endblock %}
